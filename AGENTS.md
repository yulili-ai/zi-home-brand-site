# AGENTS.md · 项目工作指令

> 本文件是对 AI 编码助手（Codex）的长期指令，每次会话生效。
> 视觉规范以 DESIGN.md 为准，文案以 CONTENT.md 为准，冲突时 DESIGN.md 优先。

## 1. 项目背景

智爱家（ZI HOME）品牌宣传网站的个人概念作品，非自如官方发布；
智爱家为自如旗下艺术智能整装品牌。
单页滚动式官网，当前精简为 4 个区块，结构见 DESIGN.md §6。
目标：让访客看完想「预约体验」——一切设计与文案服务于这个转化目标。

## 2. 文档优先级

1. DESIGN.md —— 视觉、色彩、动效、交互的唯一标准，修改样式前必须先读
2. CONTENT.md —— 品牌事实、已确认文案和当前编辑方向的依据；
   后续体验性文字可按版式优化，不必逐字照抄。禁止编造品牌事实、
   数字和服务承诺，也不使用 lorem ipsum
3. 本文件 —— 工作方式与工程纪律

## 3. 外部素材调用规则

- 缺失素材可从以下免费可商用素材库获取：图片 Unsplash、Pexels；视频 Pexels；
  图标 Lucide；音频 Pixabay、Freesound（仅 CC0）
- 获取的图片、视频、音频必须下载到项目根目录 images/ 并以相对路径本地引用，禁止热链外部素材 URL
- 仅使用明确标注“免费可商用/无需署名”许可的素材，并在 README.md
  或 CONTENT.md 记录来源链接备查
- 选材须符合 DESIGN.md §0/§2：暖调、自然光、真实家居场景、木质/亚麻材质；
  禁止冷色调、科技蓝、棚拍产品图、人物正脸特写
- 所有外部图片入库后按 DESIGN.md §2 统一叠加暖滤镜，保证色调一致
- 新增的指定手写字体只用明确可商用的开源授权（如 SIL OFL）；
  下载为本地文件并保存许可与来源，不得仅以系统楷体回退冒充。
  既有通用字体按 DESIGN.md §3 的约定继续使用
- 找不到风格匹配素材时，回退为本地色块占位并附文字说明，不硬凑

## 4. 文案工作规则

- 产品与服务的展示性标题、短句和断句允许编辑优化；同步更新 CONTENT.md
  的当前文案记录，避免页面与文档不一致
- 品牌身份、五个正式业务名称、电话、面积、交付周期、服务时效、
  真实案例资料等事实不得编造；无来源时删除或标为【待核实】，不放上页面
- 若未来确需虚构示例内容，代码注释标记 `// DEMO` 方便替换；
  一般的编辑性标题和短句不属于模拟品牌事实

## 5. 交互与动效的开源优先规则（重要）

- 实现任何动效/交互/音频效果前，先检索是否有成熟的 GitHub 开源库，
  优先引入而非手写
- 只允许引入：GitHub stars > 1k、近 12 个月有维护、
  MIT/Apache/BSD 等宽松许可的库；GSAP（greensock/GSAP）为例外放行项
- 页面不安装 npm 依赖；外部 JavaScript 库一律通过 CDN 引入，优先
  cdnjs 或 jsDelivr，并在 URL 中锁定具体版本，禁止 latest 或未锁版本地址
- 引入新的 CDN 库前，先在回复中说明库名、GitHub 地址、版本、选型理由、
  gzip 体积，经用户确认后再写入页面；已确认并已引入的库可以继续复用
- 手写仅限于：业务特定的编排逻辑（GSAP 时间线、ScrollTrigger
  场景编排）和 50 行以内的简单 CSS/光标效果
- 推荐清单（同等需求优先从这里面选，页面通过 CDN 加载）：
  - 平滑滚动：Lenis（darkroomengineering/lenis）
  - 滚动动画 / pin / scrub：GSAP + ScrollTrigger（greensock/GSAP）
  - 图标：Lucide（lucide-icons/lucide；禁止使用 lucide-react）
- 禁止引入：UI 组件库（antd/MUI/Bootstrap 等）、粒子库、3D 引擎
  （本项目无 3D 需求，见 DESIGN.md §5 禁止项）

## 6. 工作方式

- 默认每次只构建一个区块，做完即停，等待确认；若用户明确要求先看
  完整页面，则可以在同一轮完成剩余区块并做全页检查，不再逐屏暂停
- 每完成一个区块 commit 一次，commit message 格式：
  feat: <区块名>（如 feat: hero-section）
- 本项目无构建步骤；每轮完成后双击 index.html 检查页面，核对浏览器
  控制台、本地图片与 CDN 脚本加载，并对照 DESIGN.md §8 自查，汇报结果
- 发现 DESIGN.md / CONTENT.md 之间、或文档与现有代码冲突时，
  停下来报告冲突点并给出建议，不要自作主张
- 需要修改设计时，先修改对应 md 文档再改代码，保持文档与代码同步

## 7. 质量红线

- 页面按 1440px 桌面画布设计，不做响应式重排；小屏整体 transform: scale()
  等比缩放保底，不破版即可
- 本地打开与滚动流畅即可；不新增 pin+scrub 或多余滚动特效
- 尊重 prefers-reduced-motion；表单可键盘操作；图片必须有 alt
- 图片使用 WebP 并懒加载；当前页面不提供环境音
- console 零报错零警告

## 8. 部署约束

- 最终通过 GitHub Pages 部署为公开链接；仓库根目录 index.html 为入口，
  不依赖任何构建步骤
- 所有本地资源引用必须使用相对路径并以 ./ 开头，例如
  ./images/example.jpg、./main.js；禁止以 / 开头的根路径绝对引用
- CDN 链接必须使用 https，并锁定版本号
